[project]
name = "codyasbin"
version = "0.0.1"
description = "A Python package for codyasbin"
authors = [
   { name="asbin magar", email="himalthapa346@gmail.com" }
]
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
